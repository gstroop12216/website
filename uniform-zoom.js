const zoomButtons=document.querySelectorAll(".uniform-zoom");if(zoomButtons.length){const dialog=document.createElement("dialog");dialog.className="uniform-dialog";dialog.innerHTML="<button class=uniform-dialog-close type=button aria-label=Close>&times;</button><img alt>";document.body.append(dialog);const zoomedImage=dialog.querySelector("img"),closeButton=dialog.querySelector(".uniform-dialog-close");zoomButtons.forEach(button=>button.addEventListener("click",()=>{const image=button.querySelector("img")||button.parentElement.querySelector("img");zoomedImage.src=button.dataset.zoomSrc||image.src;zoomedImage.alt=image.alt;dialog.showModal();}));closeButton.addEventListener("click",()=>dialog.close());dialog.addEventListener("click",event=>{if(event.target===dialog)dialog.close();});}